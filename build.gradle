plugins {
    id 'java'
    id 'org.springframework.boot' version '3.3.8'
    id 'io.spring.dependency-management' version '1.1.7'
    id 'com.diffplug.spotless' version '6.25.0'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(17)
    }
}

repositories {
    mavenCentral()
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
    implementation 'org.springframework.boot:spring-boot-starter-validation'
    implementation 'org.seleniumhq.selenium:selenium-java:4.49.0'
    runtimeOnly 'org.seleniumhq.selenium:selenium-devtools-v151:4.49.0'

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
    testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
}

tasks.named('test') {
    useJUnitPlatform {
        excludeTags 'cdp-smoke'
    }
}

tasks.register('cdpSmokeTest', Test) {
    description = 'Runs the Chrome DevTools Protocol smoke test against example.com.'
    group = 'verification'
    useJUnitPlatform {
        includeTags 'cdp-smoke'
    }
    shouldRunAfter tasks.named('test')
}

tasks.named('compileJava') {
    dependsOn tasks.named('spotlessApply')
}

tasks.named('compileTestJava') {
    dependsOn tasks.named('spotlessApply')
}

spotless {
    java {
        importOrder(
                'java|javax|jakarta',
                'org.springframework',
                'lombok',
                '',
                'org.junit|org.mockito',
                '\\#',
                '\\#org.junit'
        )
        googleJavaFormat()
        formatAnnotations()
        removeUnusedImports()
        trimTrailingWhitespace()
        endWithNewline()
    }
}
